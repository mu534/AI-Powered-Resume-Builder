# Environment Variables

#########################
# Frontend (Vite) envs
#########################
# Public API base used by the frontend (must start with VITE_ to be exposed)
VITE_API_URL=http://localhost:5000

#########################
# Backend envs (server-side only)
#########################
# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/ai-resume

# JWT secret for signing tokens
JWT_SECRET=replace_with_secure_random_value
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Origin allowed to access backend (frontend URL)
FRONTEND_URL=http://localhost:5173

# Backend port
PORT=5000

# Add other env variables as needed
