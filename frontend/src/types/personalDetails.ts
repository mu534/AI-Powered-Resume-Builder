// types.ts (or any appropriate file in your project, e.g., src/types.ts)
export interface PersonalDetails {
  firstName: string;
  lastName: string;
  jobTitle: string;
  address: string;
  phone: string;
  email: string;
}
